# Runtime
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
asyncpg>=0.29,<1.0
pydantic[email]>=2.8,<3.0
pydantic-settings>=2.4,<3.0
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
# passlib 1.7.4 (última release) es incompatible con bcrypt>=4.1: su propio
# self-test interno (detect_wrap_bug) revienta con "password cannot be longer
# than 72 bytes" porque bcrypt>=4.1 dejó de truncar silenciosamente. Fijado a
# la última versión compatible conocida.
bcrypt==4.0.1
python-dotenv>=1.0,<2.0
# Cliente HTTP async — usado por el Asistente de IA para llamar a la API de Gemini.
httpx>=0.27,<1.0

# ETL (lectura de CSV/Excel)
pandas>=2.2,<3.0
openpyxl>=3.1,<4.0

# Testing
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0

# Lint / format
ruff>=0.6,<1.0
